<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Retro Glitch Timer for OBS</title>
    <style>
        /* Подключаем пиксельный шрифт */
        @import url('https://fonts.googleapis.com/css2?family=VT323&display=swap');

        :root {
            --timer-color: #ffffff;
            --glitch-red: #ff0055;
            --glitch-cyan: #00ffcc;
            --vhs-lines: rgba(255, 255, 255, 0.05);
        }

        body {
            background-color: transparent;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
            /* Чтобы убрать любые рамки в OBS */
        }

        /* Контейнер таймера */
        .timer-container {
            position: relative;
            font-family: 'VT323', monospace;
            font-size: 140px; /* Размер можно менять под экранчик в ролике */
            color: var(--timer-color);
            text-transform: uppercase;
            letter-spacing: 8px;
            z-index: 1;
        }

        /* Эффект Glitch fragments и Pixel drift */
        #timer {
            position: relative;
            display: inline-block;
            animation: drift 4s steps(4) infinite;
            text-shadow: 
                2px 0 var(--glitch-red), 
                -2px 0 var(--glitch-cyan);
        }

        #timer::before, #timer::after {
            content: attr(data-text);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: transparent;
        }

        /* "Битые" слои (Glitch overlay) */
        #timer::before {
            left: 2px;
            text-shadow: -2px 0 #ff00c1;
            clip: rect(44px, 450px, 56px, 0);
            animation: glitch-anim 5s infinite linear alternate-reverse;
        }

        #timer::after {
            left: -2px;
            text-shadow: -2px 0 #00fff9;
            clip: rect(10px, 450px, 30px, 0);
            animation: glitch-anim 3s infinite linear alternate-reverse;
        }

        /* Текстура VHS (Scanlines) */
        .vhs-overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(
                var(--vhs-lines) 50%, 
                rgba(0, 0, 0, 0.1) 50%
            );
            background-size: 100% 4px; /* Толщина полосок VHS */
            z-index: 10;
            pointer-events: none;
            opacity: 0.6;
        }

        /* Аналоговый шум (Imperfect digital noise) */
        .noise {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://media.giphy.com/media/oEI9uWUicHkS4/giphy.gif');
            opacity: 0.03;
            z-index: 11;
            pointer-events: none;
        }

        /* Анимации */
        @keyframes drift {
            0%, 100% { transform: translate(0, 0); }
            25% { transform: translate(1px, -1px); }
            50% { transform: translate(-1px, 1px); }
            75% { transform: translate(1px, 1px); }
        }

        @keyframes glitch-anim {
            0% { clip: rect(31px, 9999px, 94px, 0); }
            20% { clip: rect(62px, 9999px, 42px, 0); }
            40% { clip: rect(15px, 9999px, 51px, 0); }
            60% { clip: rect(85px, 9999px, 8px, 0); }
            80% { clip: rect(55px, 9999px, 72px, 0); }
            100% { clip: rect(23px, 9999px, 98px, 0); }
        }
    </style>
</head>
<body>

    <div class="vhs-overlay"></div>
    <div class="noise"></div>

    <div class="timer-container">
        <span id="timer" data-text="10:00">10:00</span>
    </div>

    <script>
        // Настройки времени
        let timeLeft = 600; // 10 минут в секундах
        const timerElement = document.getElementById('timer');

        function updateTimer() {
            let minutes = Math.floor(timeLeft / 60);
            let seconds = timeLeft % 60;

            seconds = seconds < 10 ? '0' + seconds : seconds;
            const timeString = `${minutes}:${seconds}`;
            
            // Обновляем текст и атрибут для глитч-эффекта
            timerElement.textContent = timeString;
            timerElement.setAttribute('data-text
